@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/METISTargets.cmake")
check_required_components("METIS")

get_target_property(METIS_INCLUDE_DIRECTORIES METIS::METIS INTERFACE_INCLUDE_DIRECTORIES)
get_target_property(METIS_LINK_LIBRARIES METIS::METIS INTERFACE_LINK_LIBRARIES)
get_target_property(METIS_IMPORTED_CONFIGURATIONS METIS::METIS IMPORTED_CONFIGURATIONS)
foreach(config ${METIS_IMPORTED_CONFIGURATIONS})
  get_target_property(METIS_LIBRARY_${config} METIS::METIS IMPORTED_LOCATION_${config})
endforeach()
set(METIS_FOUND ON)



